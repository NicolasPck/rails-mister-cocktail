
<!-- <div class="container my-5 d-flex justify-content-center border border-secondary">
  <div class="row"> -->


  <h1><%= @cocktail.name %></h1>
    <% @cocktail.doses.each do |dose|%>
     <p><%= dose.ingredient.name %></p>
     <p><%= dose.description %></p>
     <%= link_to "remove", dose_path(dose), method: :delete, data: {confirm: "Are your sure?"} %>
     <% end %>
  </div>
</div>


    <%= link_to "Home", cocktails_path %> |

    <%= link_to "Create new dose", new_cocktail_dose_path(@cocktail) %>

